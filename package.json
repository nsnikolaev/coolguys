{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:host": "nuxt --hostname '0' --port 8000",
    "prod:host": "nuxt --hostname '0' --port 3000",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.13.0",
    "vue": "latest"
  },
  "devDependencies": {
    "sass-embedded": "^1.77.8"
  }
}
